# 📧 Join Our Newsletter - devChallenges #

Este es un componente de suscripción a un boletín informativo totalmente responsivo y accesible. El proyecto se centra en el manejo de formularios, validación básica de HTML5 y el centrado perfecto de elementos en la pantalla.

### 🚀 Instrucciones de Ejecución ###

Para visualizar este proyecto en tu entorno local:

1. **Descarga los archivos:** Asegúrate de tener el archivo `index.html`, `styles.css` y la carpeta `img` (con el logo y el favicon).
   
2. **Estructura requerida:**
    - `/raiz-del-proyecto`
      - `index.html`
      - `styles.css`
      - `/img`
        - `logo.svg`
        - `favicon.ico`
    
3. **Abrir el proyecto:** Haz doble clic en el archivo `index.html` para abrirlo en cualquier navegador moderno (Chrome, Firefox, Edge).

### 🛠️ Tecnologías y Conceptos Aplicados ###

- **HTML5 Semántico:** Estructura organizada con `<header>`, `<main>`, `<form>` y `<footer>`.
- **CSS Flexbox:**
  - Uso de `flex-grow: 1` para un **Sticky Footer**.
  - Centrado vertical y horizontal mediante un contenedor `content`.
- **Diseño Responsivo:** Implementación de `@media queries` para adaptar el tamaño de la tipografía y la tarjeta en dispositivos móviles (412px - 640px).
- **Herencia en CSS:** Uso de `font-family: inherit` para mantener la consistencia tipográfica en elementos de formulario.

### 📋 Características ###

- **Validación de Formulario:** El campo de email es obligatorio (`required`) y cuenta con una longitud mínima de seguridad.
- **Estados Interactivos:** El botón de suscripción incluye estados `:hover` y `:active` para mejorar la experiencia de usuario (UX).
- **Accesibilidad:** Uso de placeholders claros y contraste de colores optimizado según los estándares de diseño.

### ✒️ Autora ###
- **Sara Sandoval** - Desarrollo
- Proyecto basado en el reto de [devChallenges.io](https://www.devchallenges.io/).
